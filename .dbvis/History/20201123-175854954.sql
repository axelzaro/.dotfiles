SELECT
        Convert(XML,B.XML_DEFINITION_DATA),
    B.BUILD_TYPE,
    B.BUILDKEY,
    BD.*
FROM
    BUILD B
JOIN
    BUILD_DEFINITION BD
ON
    B.BUILD_ID = BD.BUILD_ID
WHERE
    B.BUILD_TYPE = 'CHAIN'